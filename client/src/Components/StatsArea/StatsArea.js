
import React, { Component } from "react";


var StatsArea = (props) =>{
    return (
      <div className = "stat-panel-viewer">
      {props.children}
      </div>
    )
  
}

export default StatsArea;
